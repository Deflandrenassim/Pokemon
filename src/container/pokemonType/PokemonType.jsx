import React from 'react';
import './PokemonType.css';

function PokemonType() {
  // const allType = [...TypePoke];
  return (
    <div />
      > {/* <button onClick={onClick} type="button" className="pokemon_type">
        {allType.map((type) => (type.name))}
      </button> */}
  );
}

export default PokemonType;
